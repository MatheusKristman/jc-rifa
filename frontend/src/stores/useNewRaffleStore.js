import { create } from 'zustand';

const useNewRaffleStore = create((set) => ({
  raffleImage: {},
  setRaffleImage: (value) => set(() => ({ raffleImage: value })),
  title: '',
  setTitle: (e) => set(() => ({ title: e.target.value })),
  subtitle: '',
  setSubtitle: (e) => set(() => ({ subtitle: e.target.value })),
  description: [],
  setDescription: (e) => set(() => ({ description: e.target.value })),
  price: '',
  setPrice: (value) => set(() => ({ price: value })),
  raffleNumbers: 0,
  setRaffleNumbers: (e) => set(() => ({ raffleNumbers: e.target.value })),
  numbersOptions: [
    25,
    50,
    100,
    150,
    200,
    250,
    300,
    350,
    400,
    450,
    500,
    550,
    600,
    650,
    700,
    750,
    800,
    850,
    900,
    950,
    1000,
    2000,
    3000,
    4000,
    5000,
    6000,
    7000,
    8000,
    9000,
    10000,
    20000,
    30000,
    40000,
    50000,
    60000,
    70000,
    80000,
    90000,
    100000,
  ]
}));

export default useNewRaffleStore;